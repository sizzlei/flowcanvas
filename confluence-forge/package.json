{
  "name": "flowcanvas-confluence",
  "version": "0.1.0",
  "private": true,
  "description": "FlowCanvas diagram editor as a Confluence Cloud Forge macro",
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "deploy": "npm run build && forge deploy",
    "install:app": "forge install",
    "tunnel": "npm run build && forge tunnel"
  },
  "dependencies": {
    "@forge/bridge": "^4.5.3"
  },
  "devDependencies": {
    "esbuild": "^0.23.1"
  }
}
